# Onload Toast Replacement

BDD coverage for v0.1.7 request-scope startup feedback.

Host SDK notifications are the primary path. Inline banners remain as a
defensive fallback when `app.notifications.show` is unavailable.
