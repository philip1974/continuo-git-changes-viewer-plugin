# amend-integration

BDD coverage for GitViewerPanel/store amend integration.

- T34: panel passes amend state to CommitEditor.
- T35: amend roundtrip shells out with `--amend` and clears amend state.
- T36: `store.clear()` resets amend state.

