# commit-amend-shell

BDD coverage for the amend shell path.

- T9: amend mode adds `--amend` before `-F -`.
- T10: vanilla commit remains unchanged.
- T11-T12: amend success/failure map through the topic-42 result shape.
- T13: the extended commit function remains backward compatible.

