# commit-integration

BDD coverage for CommitEditor integration with GitViewerPanel and store reconciliation.

- T32: GitViewerPanel renders CommitEditor.
- T33: staged count reaches the editor.
- T34: after committing the staged side of an MM file, selection reconciles to Changed mode.
- T35: store.clear resets commitMessage.
- T36: refresh ticks preserve draft commit messages.

